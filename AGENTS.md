# Agent guidance

Follow the repository guidance in `CLAUDE.md`, including its Git conventions.

Never add `Co-Authored-By: Claude ...` or other AI/tool attribution trailers to commit messages. Tool attribution belongs in tool logs, not Git history.
